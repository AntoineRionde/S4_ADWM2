{% extends "squelette.twig"%}

{% block title %}Categories{% endblock %}

{% block content %}
    <h1>Categories</h1>
<ul>
    {% for cat in categories %}
    <li>
        <h2>{{cat.titre}}</h2>
        <h3>{{cat.description}}</h3>
        {% endfor %}
</ul>
{% endblock %}
