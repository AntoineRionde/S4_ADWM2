{% extends "squelette.twig" %}

{% block content %}
    {% if error %}
    <p class="styleError">{{ error }}</p>
    {% endif %}
    <div>
        <h1>Categorie {{ id }}</h1>
        <h3><a href="{{ url_articles }}">Voir les articles</a></h3>
    </div>
{% endblock %}
